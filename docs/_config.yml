# site settings
title: Route
subtitle: The PHP League
email: mail@philbennett.co.uk
description: Fast PSR-7 routing and dispatch component including PSR-15 middleware, built on top of FastRoute.
baseurl: # the subpath of your site, e.g. /blog
url: # the base hostname & protocol for your site

# homepage tiles
tiles-count: 4

# social settings
github_url: https://github.com/thephpleague/route
slack_url: https://thephpleague.slack.com

# build settings
markdown: kramdown
highlighter: rouge

permalink: pretty

plugins:
  - jekyll-feed
  - jekyll-sitemap

sass:
  style: compressed

kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: 'highlight'
    span:
      line_numbers: false
